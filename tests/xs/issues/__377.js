/*---
description: https://github.com/Moddable-OpenSource/moddable/issues/377
flags: [noStrict]
---*/

try {
    var aString = new String('^cro[T5?s)^hz+@&|oxA9v');
    var GQkM = aString.startsWith(aString, aString.length);
    var mJGG = aString.repeat(aString.length);
    aString = JSON.stringify('DL&;|kRkX{;R^jHh');
    var DEQr = new ArrayBuffer(null);
    var cErp = aString.toUpperCase();
    var kFzn = aString.match(mJGG, mJGG);
    var AxPG = aString.includes(aString);
    AxPG = AxPG.valueOf();
    var GYFS = aString.st();
    if (aString.search(/String/i) !== 5) {
        estFailed('{DPk.!k' + aString.search(/String/i));
    }
} catch (ex) {
    sputnikException = ex;
    var pGcJ = JSON.stringify('[<');
    var ezcN = JSON.stringify('@@&#8WqzuR&}uL61%@?(3oN3');
}
var successfullyParsed = true;
var cbRa = new ArrayBuffer(null);
var TFGn = JSON.stringify('4^iq`');
var ZdRA = new SharedArrayBuffer(null);
